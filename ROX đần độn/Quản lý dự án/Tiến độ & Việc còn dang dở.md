# Tiến độ & Việc còn dang dở

Thuộc [[Mục lục]]. Gộp toàn bộ mục "còn dang dở"/"việc cần làm tiếp theo" rải rác trong các ghi chú chức năng vào 1 chỗ. Chi tiết từng mục xem ở ghi chú chức năng tương ứng (link kèm theo).

> Cập nhật lớn nhất **2026-08-03**: đổi hẳn tab "Ải" từ map AFK-farm điều khiển được sang bảng "Ủy Thác" + hệ "Treo máy" thật; xây THẬT tab "Ải Boss" và tab "Nhân vật" (trước đó cả 2 chỉ là placeholder rỗng); thêm hệ EXP/Cấp độ đầu tiên của game (ảnh hưởng chỉ số chiến đấu thật). Xem đầy đủ ở [[Ủy Thác, Boss & Treo máy]].
>
> Cập nhật tiếp theo **2026-08-08**: đổi HẲN cách tính thưởng Treo máy - bỏ công thức chu kỳ/thời gian trôi qua, chuyển sang 1 trận đấu THẬT chạy nền liên tục, quái chết thật cộng thưởng thật ngay lúc đó (đúng ý tưởng gốc "tính riêng theo từng quái hạ được" đã từng ghi là chưa làm); chỉ còn 1 team treo máy (bỏ nhiều-team/nhiều-map); màn "Xem treo máy" giờ CHÍNH LÀ trận đó (reparent, không tạo bản giả lập riêng nữa); kèm loạt fix animation (đạn bay Cung/Pháp, xác chết tự ẩn sau 2s, không còn đứng hình ở tư thế đánh cuối) + dọn code trùng lặp. Xem đầy đủ ở [[Ủy Thác, Boss & Treo máy]].
>
> **Ghi chú (chưa rõ ngày chính xác, giữa 2 lần cập nhật trên và bản mới nhất)**: project đã được TÁI CẤU TRÚC toàn bộ từ `scripts/` phẳng sang module `core/`/`entities/`/`features/`/`legacy/` (xem `PROJECT_INDEX.md` ở gốc repo - đọc file đó trước khi mở code, mọi đường dẫn `scripts/...` trong các ghi chú CŨ ở vault đều đã LỖI THỜI). Kèm theo: gộp "Ải Boss" thành sub-tab thứ 3 trong tab "Ải" (không còn tab riêng ở BottomNav), thêm nhân vật Soldier #2, đổi combat sang nhiều ĐỢT quái/tầng (`EncounterGenerator`, camera tự bám đội hình kiểu auto-runner), và bắt đầu hệ nguyên liệu/kho (`MaterialDatabase`, `WarehousePanel`/`UpgraderPanel` trong tab Thành) - lúc đó CHƯA có nguồn rớt đồ thật.
>
> Cập nhật **2026-08-17**: thêm nguồn rớt đồ THẬT - hạ 1 quái (đánh tay hay treo máy) có **70% rơi 1 nguyên liệu cấp 1** (ngẫu nhiên trong 10 nhóm, `MaterialDatabase.random_tier1_id()`, gọi từ cả `StageFarmWorld`/`BattleScene`). Tách tab "Kho" (`inventory`, trước là placeholder tĩnh) thành module riêng `features/inventory/InventoryScreen.gd` - đọc thật `GameState.materials`, cùng dữ liệu với "Kho báu" trong tab Thành nhưng KHÔNG dùng chung code (2 feature không được gọi thẳng nhau). Thêm nhãn "Lv.X" cạnh thanh máu mỗi đơn vị trên chiến trường (`TroopUnit.level_label` - quái dùng `monster_level` thật từ `EncounterGenerator`, phe mình dùng Base Lv thật).
>
> Cập nhật tiếp theo **2026-08-17 (cùng ngày)**:
> - **Kho (`inventory`)**: nâng cấp P0 theo đề xuất `de_xuat_cai_thien_kho_do` - 2 tab "Tất cả"/"Nguyên liệu", viền ô đổi màu theo cấp (`MaterialData.tier`), bấm ô mở popup chi tiết + nút "Ghép lên cấp kế". Sửa layout lưới: `_grid` (`GridContainer`, 8 cột) có bề rộng CỐ ĐỊNH `GRID_WIDTH=442px` (ép qua `custom_minimum_size.x`, không co theo số món đang có) rồi canh giữa trong card - hàng thiếu ô không còn dồn về trái dư khoảng trống lớn bên phải, không cần ô filler.
> - **Bug "Boss nhảy sang map cũ"**: `features/combat/BattleScene.gd` (dùng cho MỌI trận Boss) thiếu hẳn cơ chế nền bám/giới hạn camera mà `StageFarmWorld` đã có, nên chỉ sau đúng 1 đợt quái, camera cuộn qua khỏi nền cố định 640px và lộ nền đen phía sau. Thiết kế lại CẢ HAI engine theo hướng "CombatArea nhỏ, cố định" (khác hẳn `_wave_anchor_x` cộng dồn vô hạn như trước): đợt quái thường xoay vòng qua 3 vị trí X cố định (`TRASH_WAVE_SLOTS`/`WAVE_SLOT_COUNT`), boss (`BattleScene`) luôn đứng ở 1 vị trí xa nhất cố định, `Camera2D.limit_left/right` chặn cứng camera không vượt quá nền (nền giờ TĨNH, đúng kích thước, không còn hack "bám theo camera mỗi frame" ở `StageFarmWorld` nữa), kèm kỹ thuật "floating-origin recenter" (dịch cả party+camera lùi lại đúng lúc hết quái giữa 2 đợt) để world-X không bao giờ tăng vô hạn dù 1 tầng có tới 29 đợt (floor cao ở Treo máy). KHÔNG gộp 2 combat engine, KHÔNG tạo class `WaveManager` mới, KHÔNG đổi `StageData`/`EncounterGenerator`/`GameState`/`TroopUnit`.
> **(ĐÃ THAY THẾ HOÀN TOÀN bởi bản "treadmill" ngày 2026-08-18 bên dưới - `TRASH_WAVE_SLOTS`/`WAVE_SLOT_COUNT`/`_recenter_world`/camera bám-1-nhân-vật đều đã bị xoá khỏi code, không còn tồn tại.)**
>
> Cập nhật lớn tiếp theo **2026-08-18** - thiết kế lại HẲN mô hình bản đồ chiến đấu theo hướng "treadmill" (tham khảo game auto-battler/side-scroller kinh điển, đơn giản hơn hẳn bản "CombatArea xoay vòng" ở trên):
> - **Camera CỐ ĐỊNH VĨNH VIỄN** (bỏ hoàn toàn camera bám-1-nhân-vật kiểu "Taskbar Heroes") - phe mình gần như đứng yên tại chỗ, CHỈ phe ĐỊCH mới thật sự di chuyển khi ngoài tầm đánh (phe mình ngoài tầm chỉ chạy animation đi bộ tại chỗ, không đổi `.position`). Quái MỌI đợt (kể cả đợt đầu, kể cả floor cao hàng chục đợt ở Treo máy) đều spawn tại ĐÚNG 1 vị trí CỐ ĐỊNH ngoài khung hình (boss ở `BattleScene` xa hơn 1 khoảng cố định) rồi tự đi vào - không còn world dài ra cần bám/giới hạn/recenter phức tạp như bản trước nữa, đơn giản hẳn.
> - **Fix ngay trong ngày**: bản đầu khoá CỨNG "phe mình tuyệt đối không di chuyển" gây bug thật - quái tầm xa dừng đánh từ khoảng cách LỚN HƠN tầm đánh của lính mình thì lính mình bị đứng chịu trận, không bao giờ vào được tầm để đánh trả. Sửa lại: phe mình chỉ bị khoá đứng yên MIỄN LÀ mục tiêu đang nhắm còn ở xa (chưa qua `free_movement_x` = ranh giới 1/4 bên phải khung hình camera, tính theo viewport thật) - quái vượt qua ranh giới đó thì 2 bên tự do di chuyển bình thường như cũ, vẫn giữ đúng hiệu ứng "quái từ xa đi vào" lúc đầu.
> - **Gộp `core/combat/CombatResolver.gd`** (mới) - chứa TOÀN BỘ hành vi chiến đấu của 1 đơn vị (targeting, né nhau/Local Avoidance, đòn đánh thường + skill "Đánh mạnh", đạn bay/hiệu ứng, rớt nguyên liệu, thưởng EXP/vàng) dùng CHUNG cho cả `BattleScene`/`StageFarmWorld` - đây là đặc trưng của NHÂN VẬT, không phải của map, nên tách riêng khỏi phần "map" (camera/spawn/timing/thắng-thua/vòng đời) vẫn giữ ở 2 file riêng. **Hệ quả: Treo máy (`StageFarmWorld`) giờ có ĐẦY ĐỦ skill "Đánh mạnh" + né nhau khi di chuyển** - trước đây thiếu 2 thứ này chỉ vì code viết trùng lặp riêng (lười viết chung), không phải chủ đích thiết kế "map treo máy không nên có skill" - đây là quyết định CÓ CHỦ Ý bật thật lên, không phải side-effect ngoài ý muốn.
> - **Thêm "chồng đợt"** (`BattleScene` - Ải Boss): mỗi đợt thường có đồng hồ đếm ngược `WAVE_TIMEOUT` (10s) riêng - đánh không kịp thì đợt kế vẫn spawn CHỒNG LÊN đợt cũ (không xoá quái cũ), càng đánh chậm càng dồn thêm quái, khó hơn hẳn. Ghi nhận qua test: vì quái giờ phải đi bộ 1 đoạn khá xa mới tới nơi đánh được, thời gian đi đường ăn khá nhiều vào 10s này nên "chồng đợt" kích hoạt thường xuyên hơn dự tính (có thể dồn 15+ quái cùng lúc) - không phải lỗi, là điểm cân bằng cần để ý nếu chỉnh `WAVE_TIMEOUT`/tốc độ di chuyển sau này.
> - **Nâng cấp tiếp - "dàn quân 3 giai đoạn"** (áp dụng cả `BattleScene` lẫn `StageFarmWorld`, mỗi đợt trong 1 tầng): thay hẳn cơ chế "free_movement_x cố định = 1/4 khung hình" ở trên bằng state machine `WaveStagingState` do MAP tự điều khiển - (1) **CENTER_WAIT**: đầu đợt, phe mình đứng GIỮA khung hình, giả vờ đi bộ (animation, không đổi vị trí) trong lúc quái còn ở xa ngoài khung hình; (2) **RETREATING**: quái sắp chạm mép phải khung hình (còn cách `RETREAT_LEAD_MARGIN`=250px) -> phe mình LÙI THẬT về 1/4 bên trái khung hình (map tự set `.position`, `CombatResolver` không có khái niệm "lùi xa" nên không thể diễn tả qua AI thường); (3) **ENGAGED**: quái đã THẬT SỰ lọt vào khung hình -> bỏ hẳn ràng buộc, 2 bên tự do lao vào nhau. `CombatResolver.run_ai()` nhận `free_movement_x` = `-INF` (CENTER_WAIT/RETREATING, chặn AI tự ý di chuyển đè lên map) hoặc `+INF` (ENGAGED, thả tự do) thay vì 1 con số cố định như bản trước. Bug đã gặp lúc làm: `WAVE_SPAWN_MARGIN` (150) nhỏ hơn `RETREAT_LEAD_MARGIN` (250) khiến quái spawn ra đã nằm sẵn trong vùng kích hoạt lùi, bỏ qua hẳn CENTER_WAIT - sửa bằng cách tăng `WAVE_SPAWN_MARGIN` lên 500 (phải luôn lớn hơn `RETREAT_LEAD_MARGIN`).
> - **Chính xác hoá lại "dàn quân 3 giai đoạn" thành 4 giai đoạn + đội hình thật + vùng debug** (cùng ngày 2026-08-18, thay thế các mốc gần đúng ở mục trên bằng công thức CHIA VÙNG rõ ràng): khung hình camera (rộng `2*viewport_half_width`) chia làm **4 phần bằng nhau theo chiều ngang** (Cam-1..Cam-4, trái→phải), khu Spawn (ngoài khung hình, phải Cam-4, tổng bề rộng `WAVE_SPAWN_MARGIN`=500) chia làm **2 phần** (Spawn-1 gần/Spawn-2 xa - quái spawn ở Spawn-2). Mốc đổi CHÍNH XÁC hơn hẳn bản gần đúng trước: **RETREATING** kích hoạt khi quái lọt qua ranh giới Spawn-1/Spawn-2 (không còn hằng số rời `RETREAT_LEAD_MARGIN`, suy thẳng từ `WAVE_SPAWN_MARGIN/2`), lùi về **GIỮA Cam-1** (không phải ranh giới Cam-1/Cam-2 như bản trước); **ENGAGED** kích hoạt khi quái tới **GIỮA Cam-4** (không phải vừa chạm mép khung hình - quái phải vào SÂU hơn mới phá đội hình). Phe mình giờ đứng đúng **đội hình "2-3-2 hình lục giác"** (`FORMATION_OFFSETS`, hàng sau 2 + giữa 3 rộng nhất + trước 2, gán theo index tạm - CHƯA có logic ai đứng vị trí nào) thay vì rải tròn ngẫu nhiên như trước - áp dụng cho CẢ `BattleScene` lẫn `StageFarmWorld`. Thêm state thứ 4 **REFORMING**: hết 1 đợt (còn đợt kế) không còn nghỉ 1 khoảng cố định (`WAVE_CLEAR_DELAY`/`_wave_transition_timer` - đã XOÁ khỏi cả 2 file) mà chuyển hẳn sang REFORMING - phe mình giả vờ tiến thêm về bên phải 1 nhịp ngắn (`REFORM_FAKE_WALK_DURATION`=0.4s, animation) RỒI mới thật sự đi (lerp theo tốc độ riêng từng người) về ĐÚNG 7 chỗ đội hình tại CENTER_WAIT - đợt kế chỉ spawn khi TẤT CẢ đã về đúng chỗ thật sự (không phải hết giờ đồng hồ cố định). Thêm `DEBUG_TEST_MODE` (const, mặc định `false`): bật lên thì camera phóng to ra (chỉ hiển thị, KHÔNG đổi mốc/logic thật) để thấy toàn bộ map + vẽ các đường kẻ (`ColorRect` mỏng) đánh dấu ranh giới Cam-1..4/Spawn-1..2/các mốc lùi-phá đội hình-spawn, tiện xem trực quan lúc test. **Đổi màn khởi động app**: `MainShell._ready()` giờ mở thẳng tab "Ải" (`_on_tab_selected("stage")`) thay vì "Thành phố" như trước - đổi KHÔNG ĐIỀU KIỆN (không qua debug flag).
> - KHÔNG đổi `StageData`/`EncounterGenerator`/`GameState`/`TroopUnit`/công thức sát thương.

## Trạng thái tổng quan
- **Vỏ app portrait 5-tab chạy được đầy đủ**: `MainShell.tscn` (`540x960`, `stretch/aspect=expand`) + `BottomNav` (thu gọn/mở rộng kéo tay) + `ScreenRouter` (5 tab: Thành phố/Ải/Ải Boss/Nhân vật/Cài đặt). Xem [[Vỏ ứng dụng Portrait (MainShell & Navigation)]].
- **Tab Thành phố** (`city`): `OverworldMap.tscn` KHÔNG có script - thuần cảnh trang trí (2 nhà + 2 NPC + 2 placeholder), KHÔNG điều khiển được, không combat.
- **Tab Ải** (`stage`) = `StageBoardScreen` - bảng thẻ, KHÔNG có map:
  - Sub-tab "Ủy Thác": 4 map × nhiều tầng, bấm thẻ vào thẳng tầng tiếp theo qua `BattleScene` thật. Chỉ có vai trò mở khoá tầng cho Treo máy.
  - Sub-tab "Treo máy": hệ treo máy THẬT - CHỈ 1 team tại 1 thời điểm, là 1 trận đấu thật chạy nền liên tục (`GameState._idle_farm_map`), quái chết thật cộng vàng/EXP thật NGAY LÚC ĐÓ (không qua công thức nào) - có nút "XEM" *nhúng* (reparent) đúng trận đang chạy vào màn hình để nhìn (không phải cảnh minh hoạ riêng) + "RÚT VỀ".
- **Tab Ải Boss** (`boss`) = `BossBoardScreen` - THẬT, không còn placeholder: Boss Ngày (2 boss, mỗi boss chọn độ khó Dễ/Thường/Khó, mỗi độ khó là 1 `StageData` riêng) + Boss Khủng (1 boss, không có độ khó, chưa có lịch hẹn giờ thật).
- **Tab Nhân vật** (`character`) = `CharacterBoardScreen` - THẬT, không còn placeholder: liệt kê đúng 4 party thật (không phải gacha), danh sách + chi tiết (chỉ số hiệu dụng đã cộng bonus cấp độ, ô trang bị tượng trưng, khối EXP/Cấp độ thật).
- **Tab Cài đặt**: list text tĩnh đúng yêu cầu, chưa bấm được gì.
- **Hệ EXP/Cấp độ mới** (dùng chung Ủy Thác/Boss/Treo máy): `ExpTables.gd` (129 cấp Base + 49 cấp Job, đúng số trong `ROX_EXP_Goc_Base_Job_DayDu.xlsx` chia 1/10) + `LinhData.exp_reward` + `GameState` tính cấp/EXP + công thức tăng chỉ số (Base Lv +10%/cấp cộng thẳng HP/ATK/DEF/M.DEF, Job Lv +1 ATK thẳng/cấp) áp trực tiếp vào combat thật.
- `BattleScene` (đấu 1 trận có thắng/thua/thời gian) giờ dùng CHUNG cho cả Ủy Thác lẫn Ải Boss qua 1 `StageFlowController` (đúng như kiến trúc đã chuẩn bị sẵn từ trước).
- Xác nhận qua headless import/boot (`--headless --import` + `--quit-after N`, không có ERROR) sau MỌI thay đổi lớn, cộng thêm debug script tạm (xoá ngay sau khi test) mô phỏng bấm nút/đổi tab/chạy nhiều frame combat để xác nhận hành vi UI+logic thật, không chỉ "không crash" - **vẫn CHƯA có ai F5 chơi thật bằng tay** để xác nhận cảm giác chơi/UI portrait trên máy thật.

## Việc dang dở theo tab

### Ải Boss (`boss`)
- [x] Đã xây thật - xem [[Ủy Thác, Boss & Treo máy]].
- [ ] Boss Khủng chưa có lịch hẹn giờ xuất hiện thật (dòng "Thời gian xuất hiện" là placeholder tĩnh, nút luôn bấm được).
- [ ] Chưa có giới hạn số lượt đánh Boss Ngày/ngày (reset mỗi ngày) - đánh được vô hạn lần hiện tại.
- [ ] Boss chỉ reskin quái có sẵn (`elite_orc`/`armored_orc`/`greatsword_skeleton`) - chưa có sprite/hoạt ảnh riêng cho boss.

### Nhân vật (`character`)
- [x] Đã xây thật - xem [[Ủy Thác, Boss & Treo máy]].
- [x] **2026-08-19 - Hệ trang bị thật, bấm-để-mặc kiểu AFK Arena (nền tảng xong, còn thiếu nguồn rớt đồ thật + tháo đồ)**:
  - **20 "loại trang bị"** (`core/database/EquipmentTypeDatabase.gd`, mới - tạo thẳng bằng code như `MaterialDatabase`, KHÔNG phải 20 file `.tres`): kiếm/cung/trượng (vũ khí chính), mũi tên/sách (vũ khí phụ - khiên/ngọc có slot riêng), khiên, ngọc, áo giáp×3 (nhẹ/vừa/nặng), áo khoác, giày×3, quần, găng, nhẫn×3 (nhờ `Rings.png` bổ sung - 1 spritesheet 7×7, cắt vùng bằng `AtlasTexture` thay vì tách file riêng), bùa chú (thay hẳn "vòng cổ" vì asset pack không đủ mẫu, dùng `tainguyen/Transperent` 37-42). Icon lấy từ `tainguyen/trang_bi`, `Icons_Free`, `gem`, `khac`, `Transperent`, `Rings.png` - gán tier T1-T5 XẾP BỪA theo yêu cầu người dùng, không có ý nghĩa thẩm mỹ xếp hạng.
  - **3 NHÓM CHIẾN ĐẤU** (`Enums.CombatGroup`: MELEE/RANGED_PHYSICAL/RANGED_MAGIC/UNIVERSAL) quyết định trang bị nào mặc được cho ai - áp dụng cho vũ khí chính/phụ + áo giáp/giày (mỗi loại gán CỨNG 1 nhóm phù hợp lối đánh, VD áo giáp nhẹ=phép/vừa=vật lý/nặng=cận chiến), còn nhẫn/bùa/áo khoác/quần/găng là UNIVERSAL (dùng chung mọi nhóm, đúng yêu cầu ban đầu). `GameState.get_combat_group_for_troop()` suy nhóm theo `character_key` ("archer"->vật lý, "wizard"/"priest"->phép, còn lại->cận chiến) - CỐ Ý KHÔNG suy theo `troop_type`/`damage_type` vì cả Archer lẫn Wizard đều là `Enums.TroopType.ARCHER`, và `archer.tres` có lỗi kế thừa `damage_type=MAGIC` dù là cung thủ vật lý (chưa sửa) - 2 field đó không đáng tin, bảng cứng theo party CỐ ĐỊNH (không gacha) là an toàn.
  - **`entities/equipment/`** (mới, module riêng theo đúng quy ước 1 module = 1 README) - `EquipmentTypeData.gd` (1 LOẠI, VD "Kiếm" - `RefCounted` thuần như `MaterialData`, không phải Resource) và `EquipmentItemData.gd` (1 món CỤ THỂ đang sở hữu - `instance_id` riêng, KHÔNG stack như nguyên liệu Kho dù trùng loại, có `quality`/`tier`/`refine_level`/`enhance_level` riêng).
  - **`GameState`** thêm `equipment_inventory` (túi đồ chưa mặc) + `equipped` (troop_id -> slot_type -> instance_id) + các hàm `add_equipment_item`/`equip_item`/`get_equipped_item`/`get_owned_equipment_for_slot`/`get_combat_group_for_troop`.
  - **`CharacterBoardScreen.gd`**: 11 ô trang bị giờ bấm được - bấm mở bảng chọn (lưới `EquipmentItemSlot`) lọc đúng túi đồ theo slot + phù hợp nhóm chiến đấu của nhân vật đó, bấm 1 món để mặc, tự vẽ lại ô ngay (icon thật + viền chất lượng + tier/tinh luyện/cường hóa) - giống hệt luồng AFK Arena người dùng mô tả. Ô CHƯA mặc vẫn hiện icon nét ngoài tượng trưng cũ (`EquipmentIcon.gd`) làm placeholder.
  - Verify: mô phỏng headless (20 loại tạo đúng, lọc slot+nhóm đúng, 100 icon load không lỗi, mapping nhóm đúng cho 4 thành viên) + chụp ảnh thật màn hình chi tiết nhân vật sau khi mặc thử 1 thanh kiếm - icon/viền/huy hiệu hiện đúng trong layout thật.
  - **Phát hiện lúc build, đã tự sửa**: `Enums.EquipmentQuality` ban đầu định khai báo trong `EquipmentItemSlot.gd` (features/character) nhưng `EquipmentDropTable.gd` (core/combat) cũng cần dùng - `core/*` không được phép import ngược `features/*` (luật CLAUDE.md) nên dời enum sang `core/config/Enums.gd`, cùng chỗ với 2 enum mới `CombatGroup`/`EquipmentSlotType`.
  - **CHƯA LÀM**: nối `EquipmentDropTable` (công thức xác suất đã có) vào `CombatResolver` để giết quái thật sự rớt đồ vào `equipment_inventory` (hiện túi đồ luôn rỗng trừ khi tự gọi `add_equipment_item` debug) - loại nào rớt được từ quái nào chưa quyết; nút "tháo trang bị" (hiện chỉ có mặc/thay, món cũ tự động rớt khỏi `equipped` nhưng người chơi không chủ động tháo tay được); trang bị chưa cộng chỉ số gì vào nhân vật (effective_hp/atk/def chưa đọc `equipped`).
- [ ] EXP/Cấp độ mới ảnh hưởng HP/ATK/DEF/M.DEF - CHƯA ảnh hưởng crit/tốc đánh/né/hồi máu/xuyên giáp/hút máu (chốt phạm vi, không phải thiếu sót).
- [ ] Job Lv mới hỗ trợ "First Job" - CHƯA có cơ chế chuyển job (Second/Third/Fourth Job có số liệu sẵn trong `ExpTables` nguồn xlsx nhưng chưa nạp/chưa có gameplay chuyển job).

### Cài đặt (`settings`)
- [ ] List tĩnh đã đúng yêu cầu hiện tại - CHƯA cần thêm logic trừ khi được yêu cầu.

### Thành phố (`city`)
- [ ] Chưa có đặt/nâng cấp công trình thật - city-building đã bị cắt hẳn khỏi project từ lâu (xem [[Chuyển thể từ vrisingDanDon]]), tab này hiện thuần cảnh trang trí, KHÔNG điều khiển được nhân vật (theo yêu cầu người dùng).
- [ ] Chỉ 2/4 vị trí NPC có nhân vật thật (2 placeholder còn trống).

### Ải (`stage` - Ủy Thác + Treo máy)
- [x] Đổi hẳn kiến trúc, xem [[Ủy Thác, Boss & Treo máy]] cho chi tiết đầy đủ.
- [x] **2026-08-08**: Công thức thưởng Treo máy giờ mô phỏng đúng "tính riêng theo từng quái hạ được" (đã từng ghi là chưa làm ở bản 08-03) - không còn `reward_gold`/`time_limit` đơn giản hoá nữa, quái chết thật trong trận nền mới cộng thưởng thật.
- [x] **2026-08-08**: Màn "Xem treo máy" không còn là hình ảnh minh hoạ tách biệt - giờ `reparent()` đúng trận đang chạy nền vào màn hình, xem hay không xem không ảnh hưởng thưởng.
- [ ] **CHƯA có save/load persistence** (vẫn là việc lớn nhất còn thiếu, nhưng đổi khác đi so với trước) - riêng phần Treo máy giờ **"tắt app là mất luôn" là lựa chọn thiết kế có chủ đích** (không bù giờ, không cần persistence riêng cho nó - khác hẳn cách nghĩ "tính cả lúc tắt app" của bản 08-03). Nhưng `GameState` nói chung (vàng, EXP/cấp độ nhân vật, `highest_floor_cleared`) vẫn reset hoàn toàn mỗi lần mở lại game - cần 1 tính năng persistence riêng (lưu ra file, đọc lại lúc khởi động) nếu muốn giữ tiến độ chung xuyên suốt các lần mở app.
- [ ] Chỉ 4 map/vài tầng mỗi map có `StageData` thật - chưa có nội dung phong phú ở tầng cao.
- [x] **2026-08-18**: `StageFarmWorld` giờ có ĐẦY ĐỦ skill "đánh mạnh" + né nhau khi di chuyển, y hệt `BattleScene` - gộp chung logic chiến đấu qua `core/combat/CombatResolver.gd` (xem mục cập nhật 2026-08-18 ở đầu file).
- [x] **2026-08-19**: Sửa bug đội hình lục giác "dồn thành 1 hàng dọc" lúc RETREATING - trước đó chỉ kéo phẳng 1 trục X chung cho mọi lính về `_retreat_x`, làm mất hẳn offset X riêng từng hàng (FORMATION_OFFSETS), chỉ còn dàn theo Y. Sửa bằng cách gộp logic "di chuyển từng đơn vị về đúng chỗ đội hình" thành 1 hàm dùng chung `_move_units_to_formation()` (dùng lại được cho cả RETREATING lẫn REFORMING, tránh lặp lại đúng bug này ở 2 chỗ) - áp dụng cả `BattleScene.gd`/`StageFarmWorld.gd`.
- [x] **2026-08-19**: Thêm hiệu ứng "wipe đóng/mở" (phủ đen mờ dần) quanh MỌI điểm đổi map/đổi tầng/đổi ải - `core/shared/ScreenWipe.gd` (mới, `ColorRect` + Tween, `close()`/`open()` awaitable, dùng lại được bất kỳ đâu). 3 nơi áp dụng, mỗi nơi tự sở hữu 1 instance riêng (không gọi chéo module): `StageFarmWorld._advance_floor()` (đổi tầng - thêm cờ `_transitioning` chặn `_process()` trong lúc phủ đen, tránh đụng units đang bị `configure()` dọn dở dang), `BattleScene.start_battle()`/`_on_result_closed()` (vào/ra trận Boss), `StageBoardScreen._on_stage_item_pressed()` (đổi map treo máy - phải thêm guard bỏ qua node wipe trong vòng lặp dọn con cũ của `_mount_battle_view()`, nếu không sẽ tự xoá mất overlay của chính nó mỗi lần remount).

## Đội hình / Roster (kế thừa từ trước, cập nhật EXP/Cấp độ)
- [ ] **Chỉ 4 nhân vật cố định** (Soldier/Archer/Wizard/Priest) - chưa có cách mở rộng/đổi thành viên, dù roster có sẵn 22+ loại lính/quái để chọn.
- [x] **EXP/Cấp độ thật đã có** (xem trên) - không còn "chưa có hệ lên cấp" như note cũ.
- [ ] Chưa có hệ trang bị/gacha (đã chốt KHÔNG làm gacha - game không có chiêu mộ nhân vật).
- [ ] Số liệu chỉ số GỐC của quái/boss vẫn phần lớn là placeholder tự chia tier (trừ EXP reward đã cân theo HP mỗi loại) - chưa phải công thức cân bằng cuối cùng.
- [ ] `archer.tres` vẫn còn lỗi kế thừa: `damage_type = MAGIC` dù là cung thủ (đáng lẽ vật lý) - chưa sửa, không chặn gì.

## Điều khiển party trên map - KHÔNG CÒN ÁP DỤNG
Model "1 leader + 3 người tự bám theo" (`PartyRosterPanel`) đã bị XOÁ hoàn toàn - City tab không điều khiển được, Ải tab không còn map. `PartyRosterPanel.gd` đã xoá file (hết chỗ dùng, xác nhận qua grep trước khi xoá). Nếu sau này có map điều khiển được trở lại, đây sẽ là 1 tính năng LÀM MỚI, không phải khôi phục.

## Kỹ thuật (không phải bug, chỉ cần lưu ý)
- **Chưa có save/persistence** - vẫn là việc quan trọng NHẤT còn thiếu (xem mục Treo máy ở trên), ảnh hưởng vàng, EXP/cấp độ nhân vật, tiến độ tầng - riêng team treo máy đang treo thì "mất khi tắt app" giờ là THIẾT KẾ có chủ đích (2026-08-08), không phải phần đang chờ persistence.
- Chưa có **export preset** cho ROXdandon - chưa từng export ra `.exe`/APK thật.
- Chưa test trên **thiết bị/emulator Android thật** (LDPlayer) - mới chỉnh kích thước cửa sổ (`540x960`) cho GIỐNG LDPlayer khi chạy trên PC, chưa build/deploy thật lên emulator.
- Mockup HTML `mockups/giao-dien-hien-tai.html` vẫn còn giữ (dùng để thảo luận/xác nhận thiết kế trước khi code Godot) - có thể đã LỖI THỜI hơn code thật ở vài chỗ (đặc biệt phần Nhân vật/Boss đã có nhiều quyết định thật hơn mockup ban đầu, ví dụ ô trang bị/EXP giờ dùng số THẬT trong Godot chứ mockup vẫn ghi "tượng trưng") - tin code hơn mockup khi lệch.
